import React from 'react'

const Error = ({children}) => {
    return (
        <div>
            <p className='text-rose-400 mt-4 text-center font-bold'>{children}</p>
        </div>
    )
}

export default Error
